#Config app
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/entropyb?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: Silencer&&55

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: dc5f322981574f18b6aa
            client-secret: 5cffe0d9fcec3c4244df39a10ff7e8f62b7a6532
  flyway:
    baseline-on-migrate: true
    validate-on-migrate: false
    enabled: true
    schemas: entropyb
    locations: classpath:db/migration

  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 100MB
      max-request-size: 100MB

server:
  servlet:
    context-path: /api

app:
  auth:
    token-secret: ZGltYWdlc2hhcmUtbGVnYWN5LWJhY2tlbmQtcHJvamVjdA==
    token-expiration-msec: 36000000

  oauth2:
    auth-request-cookie-name: oauth2_auth_request
    redirect-uri-param-cookie-name: redirect_uri
    cookie-expire-seconds: 120

  file:
    upload-dir: /tmp/entropyb